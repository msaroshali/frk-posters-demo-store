{
  "name": "project",
  "version": "1.0.0",
  "description": "- get current version from git\r - go to src/main/java/conf and adjust the application.conf if needed\r - open a terminal inside of the folder that contains the project: type 'mvn jetty:run'\r - open a web browser and go to http://localhost:8080/\r - happy testing",
  "main": "index.js",
  "directories": {
    "doc": "doc"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^4.3.1",
    "jquery": "^3.4.1",
    "popper.js": "^1.15.0"
  }
}
